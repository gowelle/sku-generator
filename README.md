# gowelle/sku-generator

A Laravel package for generating unique and customizable product and variant SKUs.

## Features

- Unique SKU generation for products and variants
- Configurable prefix, suffix, and code length
- Supports custom model mappings
- Ships as a trait for easy integration into your models
- Extensible via config

## Installation

```bash
composer require gowelle/sku-generator
